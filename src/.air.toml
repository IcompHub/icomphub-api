[build]
  cmd = "gofumpt -w . && swag init --parseDependency --parseInternal -g ./cmd/main.go && go build -o ./builds/main ./cmd"
  bin = "./builds/main"
  include_ext = ["go"]
  exclude_dir = [
    "builds",
    "docs",
    "tmp"
  ]

[log]
  time = true